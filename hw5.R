################################################################################
################################################################################

# question 7:

observed <- c(18, 16, 10, 10, 16)
expected <- c(12, 12, 12, 12, 12)
df <- 4

chi_squared <- 0

for(i in 1:length(observed))
{
  chi_squared <- chi_squared + ((observed[i] - expected[i]) ^ 2) / expected[i]
}

chi_squared

pchisq(chi_squared, df, lower.tail=FALSE)

################################################################################
################################################################################


################################################################################
################################################################################

# question 8:

library(MASS)

table <- table(survey$Exer, survey$Smoke)
table

chi_squared <- chisq.test(table)

chi_squared

chi_squared$expected

chi_squared$observed

################################################################################
################################################################################
